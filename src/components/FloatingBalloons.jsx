// FloatingBalloons is now handled inside the hearts-layer in WelcomeScreen
// This component is kept as a no-op to avoid import errors in App.jsx
export default function FloatingBalloons() {
  return null;
}
